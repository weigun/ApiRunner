name: signup
ref: signup
api: \testcase\components\api.yaml

props: 
  email: "{{gen_email}}"
  password: "{{gen_pwd}}"
  password_confirmation: "{{$password}}"
  
components: 
  - name: signup-normal
    ref: normal
    props: 
      email: "{{$email}}"
    validate:
      - op: eq
        actual: "{{.StatusCode}}"
        expected: 200